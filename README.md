# Building LLM Applications

Code associated to the book AI Applications with LangChain 

## Dependency management

This fork uses `uv` with one dependency group per chapter and a single root
`uv.lock` file. See [docs/uv.md](docs/uv.md) for install, upgrade, and
verification commands.
